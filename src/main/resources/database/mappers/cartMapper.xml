<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
    <mapper namespace="com.shop.s1.cart.CartDAO">
    
    	<!-- 장바구니 등록 -->
    	<insert id="add" parameterType="CartDTO">
    		INSERT INTO CART (C_NUM,P_NUM)
			VALUES (cart_seq.nextval,#{p_num})
    	</insert>
    
    	<!-- 로그인 한사람의 장바구니에 아이디 넣어주기 -->
    	<update id="update" parameterType="CartDTO">
    		UPDATE CART SET M_ID=#{m_id} WHERE C_NUM=#{c_num};
    	</update>
    	
    	<!-- 카트확인 detail -->
    	<select id="detail" parameterType="CartDTO" resultType="CartDTO">
    		SELECT * FROM CART 
    		WHERE M_ID=#{m_id} 
    	</select>
    	
    	<delete id="delete" parameterType="CartDTO">
    		DELETE CART WHERE C_NUM=#{c_num}
    	</delete>
    	<!-- 카트목록 -->
    	<select id="list" parameterType="CartDTO" resultType="CartDTO">
			SELECT C.C_NUM,C.M_ID,C.P_NUM,(P.P_PRICE * P.P_COUNT) C_AMOUNT,P.P_NAME,P.P_PRICE,P_COUNT 
			FROM CART C LEFT OUTER JOIN PRODUCT P 
			ON (C.P_NUM=P.P_NUM) WHERE M_ID=#{m_id}
    	</select>
    </mapper>