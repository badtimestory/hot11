<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.shop.s1.shopQna.ShopQnaDAO">
		
		<select id="list" parameterType="Pager" resultType="ShopQnaDTO">
			SELECT*FROM
			(SELECT ROWNUM R,B.*FROM
			(SELECT*FROM SHOPQNA WHERE NUM>0 ORDER BY NUM DESC)B)
			WHERE R BETWEEN #{startRow} AND #{lastRow}
		</select>
		
		<select id="total" resultType="LONG">
			SELECT COUNT (NUM) FROM SHOPQNA
			WHERE NUM>0 AND
		<choose>
			<when test="kind=='col1'">
				TITLE
			</when>
			<when test="kind=='col2'">CONTENTS</when>
			<otherwise>
				WRITER
			</otherwise>
		</choose>
		LIKE '%'||#{search}||'%'
			
		</select>
		
		<select id="detail" parameterType="ShopQnaDTO" resultType="ShopQnaDTO">
			SELECT*FROM SHOPQNA WHERE NUM=#{num}
		</select>
		
		<insert id="add" parameterType="ShopQnaDTO">
			INSERT INTO SHOPQNA(NUM, TITLE, CONTENTS, WRITER, REGDATE, HIT, REF, STEP, DEPTH)
			VALUES (SHOPQNA_SEQ.NEXTVAL, #{title}, #{contents}, #{writer}, sysdate, 0, SHOPQNA_SEQ.CURRVAL, 0, 0)
		</insert>
		
		<delete id="delete" parameterType="ShopQnaDTO">
			DELETE SHOPQNA WHERE NUM=#{num}
		</delete>

		<update id="update" parameterType="ShopQnaDTO">
			UPDATE SHOPQNA SET TITLE=#{title}, CONTENTS=#{contents}, WRITER=#{writer}
			WHERE NUM=#{num}
		</update>
</mapper>